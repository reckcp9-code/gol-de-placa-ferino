{
  "name": "gol-de-placa-ferino",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --remote --file=./schema.sql --yes"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
